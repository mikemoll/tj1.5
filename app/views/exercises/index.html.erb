<%- model_class = Exercise -%>
<div class="page-header">
  <h1><%=t '.title', :default => Exercise %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:exercise) %></th>
      <th><%= model_class.human_attribute_name(:load_prescribed) %></th>
      <th><%= model_class.human_attribute_name(:sets) %></th>
      <th><%= model_class.human_attribute_name(:reps) %></th>
      <th><%= model_class.human_attribute_name(:tempo) %></th>
      <th><%= model_class.human_attribute_name(:set_one_actual) %></th>
      <th><%= model_class.human_attribute_name(:set_two_actual) %></th>
      <th><%= model_class.human_attribute_name(:set_three_actual) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @exercises.each do |exercise| %>
      <tr>
        <td><%= exercise.id %></td>
        <td><%= exercise.exercise %></td>
        <td><%= exercise.load_prescribed %></td>
        <td><%= exercise.sets %></td>
        <td><%= exercise.reps %></td>
        <td><%= exercise.tempo %></td>
        <td><%= exercise.set_one_actual %></td>
        <td><%= exercise.set_two_actual %></td>
        <td><%= exercise.set_three_actual %></td>
        <td><%=l exercise.created_at, format: :long %></td>
        <td>
          <%= link_to exercise_path(exercise), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to exercise_path(exercise), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_exercise_path,
            :class => 'btn btn-primary' %>
